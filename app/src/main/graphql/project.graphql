query ProjectCreatorDetails($slug: String!) {
  project(slug: $slug) {
    creator {
      backingsCount
      launchedProjects {
        totalCount
      }
    }
  }
}

query GetProjectBacking($slug: String!) {
  project(slug: $slug) {
    id
    backing {
      id
      status
      sequence
      shippingAmount {
        amount
        currency
        symbol
      }
      location {
        id
        countryName
      }
      amount {
        amount
        currency
        symbol
      }
      paymentSource {
        ... on CreditCard {
          state
          id
          lastFour
          expirationDate
          type
        }
      }
      shippingAmount {
        amount
      }
      reward {
        id
        name
        backersCount
        description
        estimatedDeliveryOn
        items {
          nodes {
            name
          }
        }
        amount {
          amount
          currency
          symbol
        }
      }
      backer {
        name
        id
      }
    }
  }
}
