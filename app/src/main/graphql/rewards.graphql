query GetShippingRulesForRewardId($rewardId: ID!) {
    node(id: $rewardId) {
        ... on Reward {
            id
            name
            simpleShippingRulesExpanded {
                ...shippingRuleSimple
            }
        }
    }
}
